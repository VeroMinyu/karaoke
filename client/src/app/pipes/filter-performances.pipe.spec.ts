import { FilterPerformancesPipe } from './filter-performances.pipe';

describe('FilterPerformancesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPerformancesPipe();
    expect(pipe).toBeTruthy();
  });
});
